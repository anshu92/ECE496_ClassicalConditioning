<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asahoo264.ece496_cc">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".RefObjs"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConnectActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".CalibActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".Authentication"
            android:launchMode="singleInstance" />
        <activity
            android:name=".Handlers"
            android:launchMode="singleInstance" />
        <activity
            android:name=".LoginActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".SignupActivity"
            android:launchMode="singleInstance" />

        <service
            android:name=".MuseConnectionService"
            android:exported="false" />

        <activity android:name=".WristbandActivity" />

        <activity android:name=".CalibVerification" />
        <activity android:name=".CalibVerificationHandler" />
        <activity android:name=".CalibVerificationResult" />

        <activity android:name=".CalibIntro">
        </activity>
    </application>

</manifest>
